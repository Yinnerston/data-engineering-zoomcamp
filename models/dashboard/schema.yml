# sources:
#   - name: dashboard
#     database: de-project-369904
#     schema: dbt_nyin
#     loaded_at_field: _etl_loaded_at
#     tables:
#       - name: new_users
#         freshness: 
#           filter: datediff('created_at', _etl_loaded_at, current_timestamp) < 1
version: 2

models:
  - name: new_users
    columns:
      - name: id
        tests:
          - unique
          - not_null
      - name: created_at
        tests:
          - not_null
  - name: stg_users
    columns:
      - name: id
        tests:
          - unique
          - not_null